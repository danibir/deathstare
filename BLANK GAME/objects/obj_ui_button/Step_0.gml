if input > 0
	input -= 0.5
	
ready += 1/15
image_alpha = lerp(0, 1, ready)
if output = "start"
{
	text = "Start!\n(" + string(array_length(entry)) + "/" + string(instance_number(obj_plr)) + ")"
	
}
if ready >= 1 and image_blend = defaultcolor
{
	image_blend = c_dkgray
}